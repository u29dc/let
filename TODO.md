# TODO — Agent-Native Re-Architecture of `let`

Source of truth for details: `PLAN.md` (root).
Rule: work top-to-bottom. Pick the first `[ ]` ticket, implement it end-to-end, add tests, run quality gates, commit, then mark `[x]`.

Update policy:

- After every commit, update this file:
    - mark the implemented ticket from [ ] to [x]
    - add one line to the Update Log with: date/time (UTC), ticket id, commit hash, 1-sentence summary
- If blocked, add a BLOCKED note directly under the blocked ticket row (per prompt) and leave it [ ].

| Done | Ticket  | Title                                                                     | PLAN.md Reference                                                  | Definition of Done                                                                                                               |
| ---- | ------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| [x]  | LET-001 | Create shared cross-platform path module in core                          | Stage 0 · `packages/core/src/paths.ts`                             | `resolvePaths()` + `paths()` implemented with precedence + derived paths; exported from `@let/core`; documented in code comments |
| [x]  | LET-002 | Replace core area enrichment path resolution with shared paths            | Stage 0 · modify `core/pipeline/enrich/area.ts`                    | No internal path resolution remains; source DB access uses `paths()`; existing behavior preserved                                |
| [x]  | LET-003 | Replace core broadband enrichment path resolution with shared paths       | Stage 0 · modify `core/pipeline/enrich/broadband.ts`               | No internal path resolution remains; DB path uses `paths()`; existing behavior preserved                                         |
| [x]  | LET-004 | Add core path-resolution tests (dev + installed + overrides)              | Stage 0 · `packages/core/tests/paths.test.ts`                      | Tests cover: dev detection, Linux XDG defaults, macOS defaults, overrides precedence; deterministic and passing                  |
| [x]  | LET-005 | Add a test-only reset hook for cached paths (if cached singleton is used) | Stage 0 · paths caching                                            | Tests are order-independent; caching does not leak between tests                                                                 |
| [x]  | LET-006 | Implement CLI JSON envelope module                                        | Stage 0 · `packages/cli/src/envelope.ts`                           | `ok()`/`fail()`/`isJsonMode()` implemented; stdout purity guaranteed in `--json` mode; exit codes follow plan                    |
| [x]  | LET-007 | Add envelope tests (stdout is exactly one JSON object)                    | Stage 0 · `packages/cli/tests/envelope.test.ts`                    | Tests validate JSON parse + no extra stdout bytes for ok/fail cases                                                              |
| [x]  | LET-008 | Implement `defineToolCommand()` + `toolRegistry[]` single-source-of-truth | Tool Registry section · `packages/cli/src/tool.ts`                 | Commands defined via wrapper register metadata; registry is used for discovery; no parallel manual registry file                 |
| [x]  | LET-009 | Add registry drift test                                                   | Tool Registry section · registry drift test                        | Test fails if any command is not registered; ensures no metadata drift                                                           |
| [x]  | LET-010 | Implement `tools` command (capability discovery from registry)            | Stage 1 · `packages/cli/src/commands/tools/index.ts`               | `tools --json` returns sorted catalog; optional detail view works; meta.tool fields consistent                                   |
| [x]  | LET-011 | Implement `health` command (prereqs + remediation)                        | Stage 1 · `packages/cli/src/commands/health/index.ts`              | `health --json` reports status + checks + fix steps using resolved paths; correct exit codes for blocked                         |
| [x]  | LET-012 | Wire `tools` + `health` into CLI entrypoint (alongside legacy for now)    | Stage 1 · modify `packages/cli/src/index.ts`                       | New commands callable; legacy unaffected; `--json` mode obeys stdout/stderr rules                                                |
| [x]  | LET-013 | Add health tests (fixtures/temp dirs; no network required)                | Health section                                                     | Tests cover missing config, missing sources, missing DB (info), schema mismatch (blocking), permissions                          |
| [x]  | LET-014 | Implement `config show` (JSON + text)                                     | Stage 2 · `packages/cli/src/commands/config/show.ts`               | Uses resolved config path; validates config; JSON output stable; errors use codes + hints                                        |
| [x]  | LET-015 | Implement `config validate`                                               | Stage 2 · `packages/cli/src/commands/config/validate.ts`           | Returns structured validation errors; correct exit code when invalid                                                             |
| [x]  | LET-016 | Wire `config` group into CLI                                              | Stage 2 · `packages/cli/src/commands/config/index.ts` + entrypoint | `config` commands appear in `tools` catalog; sorted and consistent                                                               |
| [x]  | LET-017 | Implement `view list` (projection + filters)                              | Stage 2 · `packages/cli/src/commands/view/list.ts`                 | Supports core filters from plan; returns projection in JSON; text output uses existing formatters                                |
| [x]  | LET-018 | Implement `view detail` (full listing)                                    | Stage 2 · `packages/cli/src/commands/view/detail.ts`               | Retrieves by UUID/portalId; JSON is full object; proper NOT_FOUND error                                                          |
| [x]  | LET-019 | Wire `view` group into CLI                                                | Stage 2 · `packages/cli/src/commands/view/index.ts` + entrypoint   | `view` commands integrated; `tools` shows them; help is coherent                                                                 |
| [x]  | LET-020 | Implement `score explain` (breakdown)                                     | Stage 2 · `packages/cli/src/commands/score/explain.ts`             | Stable JSON breakdown; no judgment logic; NOT_FOUND handled                                                                      |
| [x]  | LET-021 | Wire `score` group (explain now; compute later)                           | Stage 2 · `packages/cli/src/commands/score/index.ts`               | `score explain` available; appears in tools catalog                                                                              |
| [x]  | LET-022 | Implement `assess candidates` (unassessed queue)                          | Stage 2 · `packages/cli/src/commands/assess/candidates.ts`         | Returns unassessed candidates ranked; supports basic filters; JSON stable                                                        |
| [x]  | LET-023 | Implement `assess context` (bundle)                                       | Stage 2 · `packages/cli/src/commands/assess/context.ts`            | Returns listing + score breakdown + assessment schema + absolute media paths + links                                             |
| [x]  | LET-024 | Wire `assess` group (read-only first)                                     | Stage 2 · `packages/cli/src/commands/assess/index.ts`              | `assess candidates/context` callable; no legacy assess drift                                                                     |
| [x]  | LET-025 | Implement `search resolve` (location lookup)                              | Stage 3 · `packages/cli/src/commands/search/resolve.ts`            | Works in JSON; includes useful results; rate-limit safe; errors structured                                                       |
| [x]  | LET-026 | Implement `search discover` (ID discovery)                                | Stage 3 · `packages/cli/src/commands/search/discover.ts`           | Uses config locations (and optional region filter); returns portal IDs only; no persistence                                      |
| [x]  | LET-027 | Implement `search diff` (new vs known)                                    | Stage 3 · `packages/cli/src/commands/search/diff.ts`               | Compares IDs vs SQLite; returns new/known; deterministic                                                                         |
| [x]  | LET-028 | Wire `search` group into CLI                                              | Stage 3 · `packages/cli/src/commands/search/index.ts`              | Search commands integrated; appear in tools catalog                                                                              |
| [x]  | LET-029 | Implement `fetch <ids>` (atomic acquisition; partial success)             | Stage 3 · `packages/cli/src/commands/fetch/index.ts`               | Fetch/parse/enrich/score/persist; rescoring behavior correct; partial success with `failed[]`; rate-limit safe                   |
| [x]  | LET-030 | Add fetch/search tests using fixtures/mocks (CI-safe)                     | Stage 3 + Testing section                                          | CI tests do not require network; live verification documented separately                                                         |
| [x]  | LET-031 | Implement `score compute` (rescore all)                                   | Stage 4 · `packages/cli/src/commands/score/compute.ts`             | Rescore all listings; JSON stats output; deterministic given same DB+config                                                      |
| [x]  | LET-032 | Implement `assess submit` (validated write-back)                          | Stage 4 · `packages/cli/src/commands/assess/submit.ts`             | Validates input; writes assessment; computes assessed score; updates listing; structured errors                                  |
| [x]  | LET-033 | Update `ops verify` for JSON + partial success                            | Stage 4 · modify `packages/cli/src/commands/ops/verify.ts`         | Works in `--json`; partial success; marks inactive; rate-limit safe                                                              |
| [x]  | LET-034 | Update `ops prune` for JSON + `--inactive` + partial success              | Stage 4 · modify `packages/cli/src/commands/ops/prune.ts`          | No interactive prompts; dry-run supported; structured output                                                                     |
| [x]  | LET-035 | Parity gate: end-to-end loop using only new commands (local)              | Stage 4 · Parity gate                                              | Demonstrate: tools → health → config → search → diff → fetch → view → assess context → assess submit → view                      |
| [x]  | LET-036 | Implement `export json`                                                   | Stage 5 · `packages/cli/src/commands/export/json.ts`               | Exports SQLite contents to JSON file; returns path + count; respects resolved paths                                              |
| [x]  | LET-037 | Implement `export notion` (if integration exists)                         | Stage 5 · `packages/cli/src/commands/export/notion.ts`             | Dry-run supported; partial success; rate-limit safe; structured errors                                                           |
| [x]  | LET-038 | Wire `export` group into CLI                                              | Stage 5 · `packages/cli/src/commands/export/index.ts`              | Export commands integrated; appear in tools catalog                                                                              |
| [x]  | LET-039 | Add contract tests for JSON envelope across commands (safe subset)        | Testing section · contract tests                                   | Spawn-based tests verify stdout purity + envelope shape; excludes destructive/network-only tools unless fixture-backed           |
| [x]  | LET-040 | Add CI matrix for Linux + macOS                                           | Testing + CI section                                               | CI builds, runs unit/contract tests, builds binary, runs smoke tests for tools/health in `--json`                                |
| [x]  | LET-041 | Cleanup: remove legacy CLI commands and files                             | Stage 6                                                            | Legacy entrypoints removed; only new surface remains; `tools` shows only new commands                                            |
| [x]  | LET-042 | Cleanup: remove obsolete imports/constants/path helpers                   | Stage 6                                                            | No dead path code remains; everything uses shared `@let/core/paths`                                                              |
| [x]  | LET-043 | Binary smoke tests in installed-binary mode paths                         | Stage 6 + Pathing                                                  | `bin/let health --json` works from arbitrary directory; resolved OS paths correct                                                |
| [x]  | LET-044 | Add `/let` skill file for Claude Code                                     | Stage 7 · `.claude/commands/let.md`                                | Skill implements orient→discover→diff→fetch→triage→context→assess→report loop; includes recovery table                           |
| [x]  | LET-045 | Update repo docs (`CLAUDE.md`) to reflect new toolbelt                    | Stage 7                                                            | Docs list new surface, how to run, how to bootstrap sources, how to verify                                                       |
| [x]  | LET-046 | Add optional live E2E verification notes (network-enabled)                | Web access note + Testing                                          | Document a live run procedure; keep CI deterministic; include rate-limit guidance                                                |
| [x]  | LET-047 | Migrate `shared-read.ts` to use `@let/core/paths`                         | Stage 6 · Files Removed: `shared-read.ts`                         | Remove `resolveRootDir()`, `ROOT_DIR`, `CACHE_DIR`, `DATA_DIR`, `LISTINGS_DB_PATH`, `CONFIG_PATH`; all consumers use `paths()` |
| [x]  | LET-048 | Migrate `shared-write.ts` to use `@let/core/paths`                        | Stage 6 · Files Removed: `shared-write.ts`                        | `processListing`, `saveListingsFile`, cache helpers use `paths().derived.*`; no path constants imported from `shared-read`       |
| [x]  | LET-049 | Remove legacy `view stats` and `view regions` subcommands                 | Stage 6 · Directory Structure (view only has list + detail)        | `view/index.ts` routes only `list` and `detail`; stats/regions code removed; `tools --json` matches routable surface            |
| [x]  | LET-050 | Delete stale `AGENT.md` tracking file                                     | Review findings                                                    | `AGENT.md` removed; `TODO.md` is the sole tracker                                                                               |

## Update Log (append-only)

- 2026-02-08T01:40Z LET-001..005 3528434 Cross-platform path module with tests, area+broadband migration, inline biome config
- 2026-02-08T01:48Z LET-006..007 b55010e JSON envelope module (ok/fail/isJsonMode) with 10 spawn-based tests
- 2026-02-08T02:05Z LET-008,010..012 1961eb0 Tool registry, tools command, health command, CLI wiring
- 2026-02-08T02:25Z LET-013 e79c083 Health command tests (4 spawn-based, temp dir isolation)
- 2026-02-08T03:10Z LET-014..016 8c9e73f Config show+validate commands, config group wired into CLI
- 2026-02-08T03:30Z LET-017..024 34e333d View list/detail, score explain, assess candidates/context, wired into CLI
- 2026-02-08T03:50Z LET-025..029 5c41578 Search resolve/discover/diff, new fetch command with partial success
- 2026-02-08T04:10Z LET-031..034 c656e87 Score compute, assess submit, ops verify/prune JSON support
- 2026-02-08T04:30Z LET-035 e9dbd72 Parity gate: 13-step spawn-based end-to-end test
- 2026-02-08T04:45Z LET-036..038 6077c85 Export json and export notion commands with tool registry
- 2026-02-08T05:00Z LET-039 a7a12f7 Contract tests: 13 JSON envelope tests validating stdout purity and shape
- 2026-02-08T05:10Z LET-040 2a2f6fd CI matrix: quality (ubuntu+macos) and smoke (build binary, verify JSON) jobs
- 2026-02-08T05:30Z LET-041..042 7e09266 Legacy cleanup: remove assess.ts, fetch.ts, help.ts, output/, enrich.ts, shared.ts
- 2026-02-08T05:45Z LET-043 1bdc6e6 Binary smoke tests: 7 tests verify compiled binary from arbitrary dir
- 2026-02-08T06:00Z LET-044..046 f2a7254 Skill file, CLAUDE.md update, live E2E notes, assess/summarize syntax
- 2026-02-08T12:00Z LET-009,030,047..050 Migrate shared-read/write to @let/core/paths, remove view stats/regions, registry drift test, search fixture tests
