# TODO — Agent-Native Re-Architecture of `let`

Source of truth for details: `PLAN.md` (root).
Rule: work top-to-bottom. Pick the first `[ ]` ticket, implement it end-to-end, add tests, run quality gates, commit, then mark `[x]`.

Update policy:

- After every commit, update this file:
    - mark the implemented ticket from [ ] to [x]
    - add one line to the Update Log with: date/time (UTC), ticket id, commit hash, 1-sentence summary
- If blocked, add a BLOCKED note directly under the blocked ticket row (per prompt) and leave it [ ].

| Done | Ticket  | Title                                                                     | PLAN.md Reference                                                  | Definition of Done                                                                                                               |
| ---- | ------- | ------------------------------------------------------------------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| [ ]  | LET-001 | Create shared cross-platform path module in core                          | Stage 0 · `packages/core/src/paths.ts`                             | `resolvePaths()` + `paths()` implemented with precedence + derived paths; exported from `@let/core`; documented in code comments |
| [ ]  | LET-002 | Replace core area enrichment path resolution with shared paths            | Stage 0 · modify `core/pipeline/enrich/area.ts`                    | No internal path resolution remains; source DB access uses `paths()`; existing behavior preserved                                |
| [ ]  | LET-003 | Replace core broadband enrichment path resolution with shared paths       | Stage 0 · modify `core/pipeline/enrich/broadband.ts`               | No internal path resolution remains; DB path uses `paths()`; existing behavior preserved                                         |
| [ ]  | LET-004 | Add core path-resolution tests (dev + installed + overrides)              | Stage 0 · `packages/core/tests/paths.test.ts`                      | Tests cover: dev detection, Linux XDG defaults, macOS defaults, overrides precedence; deterministic and passing                  |
| [ ]  | LET-005 | Add a test-only reset hook for cached paths (if cached singleton is used) | Stage 0 · paths caching                                            | Tests are order-independent; caching does not leak between tests                                                                 |
| [ ]  | LET-006 | Implement CLI JSON envelope module                                        | Stage 0 · `packages/cli/src/envelope.ts`                           | `ok()`/`fail()`/`isJsonMode()` implemented; stdout purity guaranteed in `--json` mode; exit codes follow plan                    |
| [ ]  | LET-007 | Add envelope tests (stdout is exactly one JSON object)                    | Stage 0 · `packages/cli/tests/envelope.test.ts`                    | Tests validate JSON parse + no extra stdout bytes for ok/fail cases                                                              |
| [ ]  | LET-008 | Implement `defineToolCommand()` + `toolRegistry[]` single-source-of-truth | Tool Registry section · `packages/cli/src/tool.ts`                 | Commands defined via wrapper register metadata; registry is used for discovery; no parallel manual registry file                 |
| [ ]  | LET-009 | Add registry drift test                                                   | Tool Registry section · registry drift test                        | Test fails if any command is not registered; ensures no metadata drift                                                           |
| [ ]  | LET-010 | Implement `tools` command (capability discovery from registry)            | Stage 1 · `packages/cli/src/commands/tools/index.ts`               | `tools --json` returns sorted catalog; optional detail view works; meta.tool fields consistent                                   |
| [ ]  | LET-011 | Implement `health` command (prereqs + remediation)                        | Stage 1 · `packages/cli/src/commands/health/index.ts`              | `health --json` reports status + checks + fix steps using resolved paths; correct exit codes for blocked                         |
| [ ]  | LET-012 | Wire `tools` + `health` into CLI entrypoint (alongside legacy for now)    | Stage 1 · modify `packages/cli/src/index.ts`                       | New commands callable; legacy unaffected; `--json` mode obeys stdout/stderr rules                                                |
| [ ]  | LET-013 | Add health tests (fixtures/temp dirs; no network required)                | Health section                                                     | Tests cover missing config, missing sources, missing DB (info), schema mismatch (blocking), permissions                          |
| [ ]  | LET-014 | Implement `config show` (JSON + text)                                     | Stage 2 · `packages/cli/src/commands/config/show.ts`               | Uses resolved config path; validates config; JSON output stable; errors use codes + hints                                        |
| [ ]  | LET-015 | Implement `config validate`                                               | Stage 2 · `packages/cli/src/commands/config/validate.ts`           | Returns structured validation errors; correct exit code when invalid                                                             |
| [ ]  | LET-016 | Wire `config` group into CLI                                              | Stage 2 · `packages/cli/src/commands/config/index.ts` + entrypoint | `config` commands appear in `tools` catalog; sorted and consistent                                                               |
| [ ]  | LET-017 | Implement `view list` (projection + filters)                              | Stage 2 · `packages/cli/src/commands/view/list.ts`                 | Supports core filters from plan; returns projection in JSON; text output uses existing formatters                                |
| [ ]  | LET-018 | Implement `view detail` (full listing)                                    | Stage 2 · `packages/cli/src/commands/view/detail.ts`               | Retrieves by UUID/portalId; JSON is full object; proper NOT_FOUND error                                                          |
| [ ]  | LET-019 | Wire `view` group into CLI                                                | Stage 2 · `packages/cli/src/commands/view/index.ts` + entrypoint   | `view` commands integrated; `tools` shows them; help is coherent                                                                 |
| [ ]  | LET-020 | Implement `score explain` (breakdown)                                     | Stage 2 · `packages/cli/src/commands/score/explain.ts`             | Stable JSON breakdown; no judgment logic; NOT_FOUND handled                                                                      |
| [ ]  | LET-021 | Wire `score` group (explain now; compute later)                           | Stage 2 · `packages/cli/src/commands/score/index.ts`               | `score explain` available; appears in tools catalog                                                                              |
| [ ]  | LET-022 | Implement `assess candidates` (unassessed queue)                          | Stage 2 · `packages/cli/src/commands/assess/candidates.ts`         | Returns unassessed candidates ranked; supports basic filters; JSON stable                                                        |
| [ ]  | LET-023 | Implement `assess context` (bundle)                                       | Stage 2 · `packages/cli/src/commands/assess/context.ts`            | Returns listing + score breakdown + assessment schema + absolute media paths + links                                             |
| [ ]  | LET-024 | Wire `assess` group (read-only first)                                     | Stage 2 · `packages/cli/src/commands/assess/index.ts`              | `assess candidates/context` callable; no legacy assess drift                                                                     |
| [ ]  | LET-025 | Implement `search resolve` (location lookup)                              | Stage 3 · `packages/cli/src/commands/search/resolve.ts`            | Works in JSON; includes useful results; rate-limit safe; errors structured                                                       |
| [ ]  | LET-026 | Implement `search discover` (ID discovery)                                | Stage 3 · `packages/cli/src/commands/search/discover.ts`           | Uses config locations (and optional region filter); returns portal IDs only; no persistence                                      |
| [ ]  | LET-027 | Implement `search diff` (new vs known)                                    | Stage 3 · `packages/cli/src/commands/search/diff.ts`               | Compares IDs vs SQLite; returns new/known; deterministic                                                                         |
| [ ]  | LET-028 | Wire `search` group into CLI                                              | Stage 3 · `packages/cli/src/commands/search/index.ts`              | Search commands integrated; appear in tools catalog                                                                              |
| [ ]  | LET-029 | Implement `fetch <ids>` (atomic acquisition; partial success)             | Stage 3 · `packages/cli/src/commands/fetch/index.ts`               | Fetch/parse/enrich/score/persist; rescoring behavior correct; partial success with `failed[]`; rate-limit safe                   |
| [ ]  | LET-030 | Add fetch/search tests using fixtures/mocks (CI-safe)                     | Stage 3 + Testing section                                          | CI tests do not require network; live verification documented separately                                                         |
| [ ]  | LET-031 | Implement `score compute` (rescore all)                                   | Stage 4 · `packages/cli/src/commands/score/compute.ts`             | Rescore all listings; JSON stats output; deterministic given same DB+config                                                      |
| [ ]  | LET-032 | Implement `assess submit` (validated write-back)                          | Stage 4 · `packages/cli/src/commands/assess/submit.ts`             | Validates input; writes assessment; computes assessed score; updates listing; structured errors                                  |
| [ ]  | LET-033 | Update `ops verify` for JSON + partial success                            | Stage 4 · modify `packages/cli/src/commands/ops/verify.ts`         | Works in `--json`; partial success; marks inactive; rate-limit safe                                                              |
| [ ]  | LET-034 | Update `ops prune` for JSON + `--inactive` + partial success              | Stage 4 · modify `packages/cli/src/commands/ops/prune.ts`          | No interactive prompts; dry-run supported; structured output                                                                     |
| [ ]  | LET-035 | Parity gate: end-to-end loop using only new commands (local)              | Stage 4 · Parity gate                                              | Demonstrate: tools → health → config → search → diff → fetch → view → assess context → assess submit → view                      |
| [ ]  | LET-036 | Implement `export json`                                                   | Stage 5 · `packages/cli/src/commands/export/json.ts`               | Exports SQLite contents to JSON file; returns path + count; respects resolved paths                                              |
| [ ]  | LET-037 | Implement `export notion` (if integration exists)                         | Stage 5 · `packages/cli/src/commands/export/notion.ts`             | Dry-run supported; partial success; rate-limit safe; structured errors                                                           |
| [ ]  | LET-038 | Wire `export` group into CLI                                              | Stage 5 · `packages/cli/src/commands/export/index.ts`              | Export commands integrated; appear in tools catalog                                                                              |
| [ ]  | LET-039 | Add contract tests for JSON envelope across commands (safe subset)        | Testing section · contract tests                                   | Spawn-based tests verify stdout purity + envelope shape; excludes destructive/network-only tools unless fixture-backed           |
| [ ]  | LET-040 | Add CI matrix for Linux + macOS                                           | Testing + CI section                                               | CI builds, runs unit/contract tests, builds binary, runs smoke tests for tools/health in `--json`                                |
| [ ]  | LET-041 | Cleanup: remove legacy CLI commands and files                             | Stage 6                                                            | Legacy entrypoints removed; only new surface remains; `tools` shows only new commands                                            |
| [ ]  | LET-042 | Cleanup: remove obsolete imports/constants/path helpers                   | Stage 6                                                            | No dead path code remains; everything uses shared `@let/core/paths`                                                              |
| [ ]  | LET-043 | Binary smoke tests in installed-binary mode paths                         | Stage 6 + Pathing                                                  | `bin/let health --json` works from arbitrary directory; resolved OS paths correct                                                |
| [ ]  | LET-044 | Add `/let` skill file for Claude Code                                     | Stage 7 · `.claude/commands/let.md`                                | Skill implements orient→discover→diff→fetch→triage→context→assess→report loop; includes recovery table                           |
| [ ]  | LET-045 | Update repo docs (`CLAUDE.md`) to reflect new toolbelt                    | Stage 7                                                            | Docs list new surface, how to run, how to bootstrap sources, how to verify                                                       |
| [ ]  | LET-046 | Add optional live E2E verification notes (network-enabled)                | Web access note + Testing                                          | Document a live run procedure; keep CI deterministic; include rate-limit guidance                                                |

## Update Log (append-only)

- (empty)
