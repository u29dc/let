{
	"name": "let",
	"version": "0.0.1",
	"type": "module",
	"private": true,
	"repository": {
		"type": "git",
		"url": "https://github.com/u29dc/let.git"
	},
	"scripts": {
		"prepare": "husky",
		"let": "cargo run -q -p let-cli --",
		"let:dev": "cargo run -p let-cli --",
		"let:tui": "cargo run -p let-tui --",
		"start": "cargo run -q -p let-tui --",
		"build": "bun scripts/release.ts",
		"build:cli": "bun run build",
		"build:sources": "cargo run -q -p let-cli -- sources build all --jobs 3",
		"build:source:broadband": "cargo run -q -p let-cli -- sources build broadband",
		"build:source:postcodes": "cargo run -q -p let-cli -- sources build postcodes",
		"build:source:deprivation": "cargo run -q -p let-cli -- sources build deprivation",
		"build:source:census": "cargo run -q -p let-cli -- sources build census",
		"build:source:population": "cargo run -q -p let-cli -- sources build population",
		"build:source:income": "cargo run -q -p let-cli -- sources build income",
		"build:source:flood": "cargo run -q -p let-cli -- sources build flood",
		"build:source:naptan": "cargo run -q -p let-cli -- sources build naptan",
		"build:source:uprn": "cargo run -q -p let-cli -- sources build uprn",
		"build:source:crime": "cargo run -q -p let-cli -- sources build crime",
		"util:format": "cargo fmt --all --check",
		"util:lint": "cargo clippy --workspace --all-targets --all-features -- -D warnings",
		"util:types": "cargo check --workspace --all-targets && bunx tsc --noEmit",
		"util:test": "cargo test --workspace",
		"util:build": "cargo build --workspace",
		"util:check": "STATUS=0; bun run util:format || STATUS=1; bun run util:lint || STATUS=1; bun run util:types || STATUS=1; bun run util:test || STATUS=1; bun run build || STATUS=1; exit $STATUS",
		"clean": "cargo clean"
	},
	"devDependencies": {
		"@commitlint/cli": "^21.0.2",
		"@commitlint/config-conventional": "^21.0.2",
		"husky": "^9.1.7",
		"lint-staged": "^17.0.7"
	}
}
